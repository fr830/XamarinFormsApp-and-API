@model IEnumerable<Sagrada.Dominio.Tarjeta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Banco.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodOperacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Banco.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodOperacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTarjeta }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTarjeta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTarjeta })
        </td>
    </tr>
}

</table>
