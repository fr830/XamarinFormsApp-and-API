@model IEnumerable<Sagrada.Dominio.CobroMedico>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Especialidad.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Medico.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Honorarios)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Especialidad.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Medico.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Honorarios)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCobroMedico }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCobroMedico }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCobroMedico })
        </td>
    </tr>
}

</table>
